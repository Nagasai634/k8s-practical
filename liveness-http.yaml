#liveness-http
apiVersion: v1 
kind: Pod
metadata:
  name: liveness-http
spec:
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 80
      livenessProbe:
        httpGet:
          path: /
          port: 80 #(container Port)
        initialDelaySeconds: 5
        periodSeconds: 5
