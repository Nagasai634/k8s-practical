apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring
spec:
  replicas: 2
  selector:
    matchLabels:
      name: spring
  template:
    metadata:
      labels:
        name: spring
    spec:
      imagePullSecrets:
        - name: gar-secret
      containers:
        - name: spring
          image: us-central1-docker.pkg.dev/intrepid-axe-482904-f4/harness-repo/app:blue
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: spring-svc
spec:
  selector:
    name: spring
  type: NodePort
  ports:
  - targetPort: 80
    port: 80
    protocol: "TCP"                
